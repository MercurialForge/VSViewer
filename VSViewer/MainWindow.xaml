<Window x:Class="VSViewer.MainWindow"
        x:Name="root"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dxc="clr-namespace:SharpDX.WPF;assembly=SharpDX.WPF"
        xmlns:local="clr-namespace:VSViewer"
        Title="MainWindow" Height="549.013" Width="1080.054">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="auto" MinWidth="200"/>
            </Grid.ColumnDefinitions>
            <Grid Background="Black" Grid.Column="0">
                <dxc:DXElement Renderer="{Binding Viewport}"/>
            </Grid>
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Pink"/>
            <Grid Grid.Column="2" Background="Red">
                <Button Content="open" Click="Read_Click"/>
            </Grid>
        </Grid>
</Window>
